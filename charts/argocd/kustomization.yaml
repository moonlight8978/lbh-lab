# yaml-language-server: $schema=https://www.schemastore.org/kustomization.json

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - releaseName: argocd
    namespace: argocd
    name: argocd
    repo: oci://ghcr.io/home-operations/charts-mirror/
    valuesFile: values.yaml
    version: 8.1.3

resources:
  - manifests/applicationset.yaml
  - manifests/externalsecret.yaml
  - manifests/projects/system.yaml
  - manifests/projects/cicd.yaml
